{
  "skill_name": "ontology-mapper",
  "evals": [
    {
      "id": 1,
      "prompt": "Map the term 'space group' to the CMSO ontology. I want to know which class and properties correspond to this concept.",
      "expected_output": "Agent runs concept_mapper.py to find CMSO classes and properties matching 'space group', returning matches with confidence scores.",
      "assertions": [
        "Invokes scripts/concept_mapper.py with --ontology cmso --term 'space group' --json",
        "Output includes results.matches with a confidence score; because the synonym table is consulted before exact-label matching, 'space group' is reported as a 0.9 synonym_class match (not 1.0), and the matched class/IRI are still correct",
        "Returns the matching CMSO class (Space Group) and related properties",
        "Reports any unmatched terms or alternative suggestions"
      ]
    },
    {
      "id": 2,
      "prompt": "I have an FCC copper crystal with lattice parameter a=3.615 angstroms and space group 225. Map these crystallographic parameters to CMSO ontology terms.",
      "expected_output": "Agent runs crystal_mapper.py to translate FCC, space group 225, and lattice constant into CMSO-compliant annotations with Bravais lattice symbol.",
      "assertions": [
        "Invokes scripts/crystal_mapper.py with --ontology cmso --bravais FCC --space-group 225 --a 3.615 --json",
        "Output includes results.ontology_classes and results.ontology_properties",
        "Maps FCC to the correct Bravais lattice symbol (cF) in CMSO terminology",
        "Includes space group mapping to number 225 (Fm-3m)"
      ]
    },
    {
      "id": 3,
      "prompt": "Annotate this sample for CMSO: {\"material\": \"iron\", \"structure\": \"BCC\", \"lattice_a\": 2.87, \"space_group\": 229, \"simulation_type\": \"MD\"}",
      "expected_output": "Agent runs sample_annotator.py with the full sample description, producing CMSO annotations for material, structure, lattice, and identifying unmapped fields.",
      "assertions": [
        "Invokes scripts/sample_annotator.py with --ontology cmso --sample '{\"material\":\"iron\",\"structure\":\"BCC\",\"lattice_a\":2.87,\"space_group\":229,\"simulation_type\":\"MD\"}' --json",
        "Output includes results.annotations with class-property mappings for iron BCC",
        "Maps iron to Fe element and BCC to cI Bravais lattice",
        "Reports unmapped_fields for any keys not recognized (e.g., simulation_type may map or appear as unmapped)"
      ]
    },
    {
      "id": 4,
      "prompt": "Map these terms all at once: FCC, copper, lattice constant, unit cell, grain boundary",
      "expected_output": "Agent runs concept_mapper.py with multiple terms to batch-map all five concepts to CMSO, identifying any that cannot be matched.",
      "assertions": [
        "Invokes scripts/concept_mapper.py with --ontology cmso --terms 'FCC,copper,lattice constant,unit cell,grain boundary' --json",
        "Output includes results.matches for successfully mapped terms",
        "Reports results.unmatched for any terms not found in CMSO (e.g., 'copper' has no CMSO class; 'grain boundary' maps to the Crystal Defect class)",
        "Provides results.suggestions for unmatched terms (a runnable class_browser.py command including --ontology) to guide the user"
      ]
    },
    {
      "id": 5,
      "prompt": "Map a hexagonal crystal: Bravais lattice HCP, a=3.21, c=5.21, space group 194. No ontology specified, just give me generic labels.",
      "expected_output": "Agent runs crystal_mapper.py without specifying an ontology to produce generic crystallographic labels, including validation of hexagonal lattice constraints.",
      "assertions": [
        "Invokes scripts/crystal_mapper.py with --bravais HCP --space-group 194 --a 3.21 --c 5.21 --json (no --ontology flag)",
        "Output includes generic crystal system identification (hexagonal)",
        "Maps HCP to the appropriate Bravais lattice symbol (hP)",
        "Validates that a != c for hexagonal system (no validation warning expected since 3.21 != 5.21)"
      ]
    },
    {
      "id": 6,
      "prompt": "Annotate this crystalline sample for the ASMO ontology: {\"material\": \"copper\", \"structure\": \"FCC\", \"lattice_a\": 3.615, \"space_group\": 225}",
      "expected_output": "Agent runs sample_annotator.py with --ontology asmo and surfaces that ASMO does not define crystal/sample vocabulary: the emitted crystal terms are flagged in results.validation_warnings rather than presented as valid ASMO annotations.",
      "assertions": [
        "Invokes scripts/sample_annotator.py with --ontology asmo --sample '{\"material\":\"copper\",\"structure\":\"FCC\",\"lattice_a\":3.615,\"space_group\":225}' --json",
        "Output includes a non-empty results.validation_warnings list noting that classes/properties such as 'Crystal Structure', 'Unit Cell', 'Space Group' (or their properties) are not defined in the loaded ontology",
        "Crystal/sample annotations for ASMO carry confidence 0.0 and a validation_warning field (they do not resolve to ASMO IRIs)",
        "Agent advises using --ontology cmso for crystalline sample annotation, reserving ASMO for the concept-mapping path"
      ]
    }
  ]
}
