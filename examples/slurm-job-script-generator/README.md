# SLURM Job Script Generator Example

This example demonstrates how to generate a portable `sbatch` script for a simulation run.

## Run

From the repository root:

```bash
bash examples/slurm-job-script-generator/run_generate_job.sh
```

## What it does

- Generates a job script using `slurm_script_generator.py`
- Prints a preview of the generated script

